ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ altak.ledger.infrastructure.ktor.ApplicationKt.rootModule ]
    }
}

ledger {
    seed = "seed.json"
}
